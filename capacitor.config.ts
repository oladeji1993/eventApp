import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hotMuzikArena',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
